# STM32F103 智慧农业终端开发进度与总体计划

- 项目：STM32F103CBT6 智慧农业终端
- 当前日期：2026-09-18
- 当前 CMake 工程：`D:\test\STM32_Cmake\Cube_MX_Code`
- Keil 基准工程：`D:\test\Cube_MX_Code`
- 当前分支：`main`
- 说明：本文件记录整个项目的开发路线、已完成内容、当前状态和后续验收标准。

## 一、项目最终目标

构建一个基于 STM32F103C8T6 的智慧农业终端，实现：

```text
环境采集
  ├─ 光照
  ├─ 土壤湿度
  ├─ DHT22 温湿度
  └─ CO2
      ↓
本地显示与状态判断
      ↓
执行器控制
  ├─ 继电器/水泵
  ├─ 电机/风扇
  └─ 蜂鸣器报警
      ↓
本地交互和远程通信
  ├─ 按键
  ├─ USART1 调试/配置
  └─ LLCC68 LoRa
```

最终应具备：

- 稳定的多传感器采集；
- OLED 实时数据显示；
- 自动/手动控制模式；
- 传感器故障检测和安全状态；
- 参数配置和保存；
- LoRa 数据上报与控制指令接收；
- CMake 和 Keil 两种构建方式的代码行为一致；
- 可长期运行并能够复现、定位和记录故障。

## 二、开发原则

1. `.ioc` 是 CubeMX 外设和引脚配置的事实来源，新增外设先由用户在 CubeMX 中手动配置。
2. 不直接修改 `cmake/stm32cubemx/CMakeLists.txt`，用户源文件加入顶层 `CMakeLists.txt`。
3. 用户业务代码放在 `User/` 或 CubeMX `USER CODE BEGIN/END` 区域。
4. 所有硬件结论以原理图、`.ioc`、生成代码和实物验证共同确认。
5. Keil 已验证代码作为硬件基准，迁移到 CMake 后必须重新编译、烧录和实物验证。
6. 不把“编译成功”当成“已经烧录成功”，必须核对 ST-Link 的实际 ELF 路径、下载和校验日志。
7. 传感器读取失败不能卡死整个系统，必须保留有效状态、错误码和更新时间。
8. 所有计划中功能和已经实物验证的功能分开记录。

## 三、硬件资源与引脚计划

### 3.1 当前已配置资源

| 功能 | 引脚 | 外设/模式 | 当前状态 |
|---|---|---|---|
| 光照 | PA0 | ADC1_IN0 + DMA | 已验证采集 |
| 土壤湿度 | PA1 | ADC1_IN1 + DMA | 已验证采集 |
| CO2 TX/RX | PA2/PA3 | USART2，9600 8N1 | 已验证，Keil版本正常 |
| LED2/PWM | PA8 | TIM1_CH1 | 已配置，业务待完善 |
| 调试串口 | PA9/PA10 | USART1，115200 8N1 | 已使用 |
| SW1/SW2 | PB12/PB13 | EXTI 下降沿 + 上拉 | 已配置 |
| OLED | PB6/PB7 | I2C1 | 已验证显示 |
| 蜂鸣器候选 | PB9 | TIM4_CH4 | 需确认有源/无源方案 |
| LED3 | PB15 | GPIO 输出 | 已配置 |
| 板载 LED | PC13 | GPIO 输出 | 心跳灯已使用 |
| DHT22 | PC15 | 单总线 | 已验证获取成功 |
| SWD | PA13/PA14 | Serial Wire | 保留 |

### 3.2 待配置资源

| 功能 | 引脚 | 计划 |
|---|---|---|
| 继电器 | PC14 | CubeMX 手动配置 GPIO 输出，默认关闭 |
| 电机 | PB8 | CubeMX 手动配置 TIM4_CH3 或 GPIO，默认停止 |
| LoRa NSS | PA4 | SPI 软件片选 |
| LoRa SCK | PA5 | SPI1_SCK |
| LoRa MISO | PA6 | SPI1_MISO |
| LoRa MOSI | PA7 | SPI1_MOSI |
| LoRa BUSY | PB0 | GPIO 输入 |
| LoRa RESET | PB1 | GPIO 输出 |
| LoRa DIO1 | PB10 | EXTI 中断 |

硬件红线：

- LSE 必须保持关闭，PC14/PC15 不得被 RTC/LSE 占用；
- PA13/PA14 不得改作普通 GPIO；
- DHT22 数据线必须有可靠上拉；
- CO2 模块电平和供电需按具体模块手册确认；
- OLED 的 PB6 是 I2C SCL，不是 SPI SCK。

## 四、当前已完成进度

### 阶段0：工程和构建基础——已完成

- [x] STM32CubeMX 工程建立；
- [x] STM32F103CBT6、HSE、PLL×9、72MHz 配置；
- [x] SWD 保留，JTAG 关闭；
- [x] CMake + Ninja + GNU Arm 构建链路建立；
- [x] 顶层 CMake 加入 `User/app`、`User/bsp`、`User/oled`；
- [x] 修复重命名工程后的 CMake 缓存和 Build Analyzer 路径；
- [x] Git 初始化、GitHub 远程绑定和首次推送；
- [x] README 建立。

### 阶段1：基础采集——已完成

- [x] ADC1 双通道扫描；
- [x] DMA1 Channel1 循环模式；
- [x] ADC 启动前校准；
- [x] 光照查表换算；
- [x] 土壤湿度等级换算；
- [x] USART1 调试输出和 `printf` 重定向；
- [x] USART2 CO2 接收框架；
- [x] DHT22 驱动；
- [x] OLED 基础显示；
- [x] PC13 心跳灯；
- [x] CMake Debug 生成 ELF/MAP。

### 阶段2：Keil/CMake 实物基准迁移——基本完成

- [x] 确认 Keil5/MDK 工程在同一块板上成功显示温度、湿度、CO2、光照和土壤值；
- [x] 对比 Keil 与 CMake 的实际源文件和 `.ioc`；
- [x] 确认两个工程此前不是同一份业务代码；
- [x] 迁移 Keil 已验证 DHT22 驱动；
- [x] 迁移 Keil 已验证 CO2 驱动；
- [x] 迁移 Keil OLED 帧缓冲驱动；
- [x] 为 CMake OLED 增加按页局部刷新；
- [x] CMake Debug 完整构建通过；
- [x] 解决 CMake/GCC 下 DHT22 微秒时序问题；
- [x] DHT22 实物读取成功。

### 阶段3：传感器服务层——已完成第一版

- [x] 新增 `User/service/sensor_service.c/.h`；
- [x] 建立 `SensorData_t` 统一状态；
- [x] ADC 16次平均滤波；
- [x] DHT22 周期采集；
- [x] CO2 采集接入服务层；
- [x] OLED 温湿度、CO2、Lux、Soil 分区显示；
- [x] USART1 周期状态报告；
- [x] 传感器有效状态和错误码；
- [x] CMake ELF 实物验证基础闭环；
- [ ] 非阻塞 CO2 双缓冲最终版；
- [ ] DHT22 更完整的硬件时序诊断；
- [ ] I2C Fast Mode 400kHz 通过 CubeMX 正式配置并重新审查。

## 五、当前实际状态

### 已经实物确认

- Keil5/MDK 版本可以显示：
  - 温度；
  - 湿度；
  - CO2；
  - 光照；
  - 土壤湿度。
- CMake 版本已迁移相同的关键 DHT22、CO2 和 OLED 驱动；
- CMake 版本可以从 Debug 配置生成 ELF 和 MAP；
- DHT22 在 CMake/GCC 下经过局部优化和临界区处理后已成功获取；
- OLED 支持按数据行对应页刷新。

### 当前仍未完成实物确认

- CMake 最新 ELF 已烧录后的完整长时间运行；
- I2C 400kHz 配置后的稳定性；
- 按键页面切换；
- 继电器、电机、蜂鸣器实际控制；
- LoRa 收发；
- 传感器断线后的完整故障恢复；
- 长时间运行和复位恢复。

## 六、后续开发计划

### 阶段4：采集和显示稳定化

1. 烧录最新 CMake ELF，逐项确认 DHT22、CO2、光照、土壤；
2. 串口记录启动、更新、错误和离线状态；
3. 在 CubeMX 中将 I2C1 改为 Fast Mode 400kHz；
4. 重新生成代码并审查 `.ioc`、`i2c.c`、`main.h`；
5. 验证 OLED 局部刷新没有残影、闪烁和覆盖；
6. 修复按键回调中断内阻塞问题；
7. 增加传感器连续失败计数和恢复计数。

### 阶段5：执行器配置与本地控制

1. 在 CubeMX 手动配置 PC14 `RELAY`；
2. 在 CubeMX 手动配置 PB8 `MOTOR`/TIM4_CH3；
3. 确认 PB9 是有源还是无源蜂鸣器；
4. 为继电器、电机、蜂鸣器增加安全初始化；
5. 实现自动/手动模式；
6. 实现土壤灌溉滞回控制；
7. 实现温度通风滞回和 PWM 调速；
8. 实现 CO2/传感器故障报警；
9. 验证复位、断线和超限时执行器进入安全状态。

### 阶段6：人机交互和参数管理

1. SW1 页面切换；
2. SW2 确认/手动控制；
3. 长按识别；
4. OLED 总览、执行器、故障和设置页面；
5. USART1 CLI：`status`、`mode`、`relay`、`motor`、`set`、`save`；
6. 阈值参数结构体；
7. 参数版本、魔数和 CRC；
8. 内部 Flash 参数保存，限制写入频率。

### 阶段7：LoRa 通信

1. CubeMX 手动配置 SPI1 和 LoRa 引脚；
2. LLCC68 复位、BUSY、寄存器和 SPI 读写；
3. 收发状态机；
4. 数据帧头、设备地址、类型、序号、长度和 CRC16；
5. 传感器数据上报；
6. 执行器控制命令；
7. ACK、超时重传和重复帧去重；
8. 离线检测和恢复；
9. 与上位机或网关联调。

### 阶段8：测试和交付

1. 构建检查：0 Error、0 Warning；
2. ELF/MAP 路径和 ST-Link 下载路径一致；
3. 传感器正常、断开、短帧、校验错误测试；
4. 阈值附近滞回测试；
5. 按键连按、长按和抖动测试；
6. OLED 长时间刷新测试；
7. LoRa 丢包、重复帧和重连测试；
8. 连续运行至少30分钟；
9. 更新 README、错误记录和版本号；
10. 提交 Git 并推送 GitHub。

## 七、当前验收命令

```powershell
cmake --fresh --preset Debug
cmake --build --preset Debug --clean-first
arm-none-eabi-size build/Debug/Cube_MX_Code.elf
git diff --check
git status
```

当前最终验收还必须结合硬件：

```text
烧录 build/Debug/Cube_MX_Code.elf
确认 ST-Link Programming/Verification
查看 USART1 输出
观察 OLED 四个数据区域
记录传感器成功和失败状态
```

## 八、版本记录

| 日期 | 版本/事件 | 结果 |
|---|---|---|
| 2026-09-17 | 完成 CMake 工程和 GitHub 初始推送 | 完成 |
| 2026-09-18 | 建立 Sensor Service 设计和第一版实现 | 构建通过 |
| 2026-09-18 | 发现 Keil 与 CMake 为不同代码快照 | 完成差异定位 |
| 2026-09-18 | 迁移 Keil 已验证 DHT22、CO2、OLED 驱动 | 构建通过 |
| 2026-09-18 | 解决 CMake/GCC DHT22 微秒时序问题 | 用户确认读取成功 |
